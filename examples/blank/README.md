# blank capsule

The smallest capsule that still does something. A single `hello` query
returns a greeting with the current server time. No tables, no
mutations, no auth.

## Run

```sh
zapdev dev
```

## What it shows

- one query, no schema
- the basic server file structure
- the basic client file structure
- a good starting point for any capsule
