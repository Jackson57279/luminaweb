# guestbook capsule

A shared guestbook with author metadata from auth.

## Run

```sh
zapdev dev
```

Open `http://localhost:3000` and try `?zapdev_guest=alice` in another tab to see author metadata vary.
